import {SearchPageState} from '../../../state';
import {createMockState} from '../../../test';

describe('range facet selectors', () => {
  let state: SearchPageState;

  beforeEach(() => {
    state = createMockState();
  });
});
